@model InteractiveNotifs.Hub.Models.NotificationViewModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<h4>NotificationViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="Notifications" asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AdaptiveBlock" class="control-label"></label>
                <textarea asp-for="AdaptiveBlock" rows="20" class="form-control"></textarea>
                <span asp-validation-for="AdaptiveBlock" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
